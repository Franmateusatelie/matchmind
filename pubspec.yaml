name: matchmind
description: "MatchMind - Palpites inteligentes de futebol"
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.10.4

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.8

  # 🌐 HTTP para consumir API de futebol
  http: ^1.2.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

  assets:
    - assets/matchmind_logo.png
    - assets/campeonatos/
    - assets/times/
